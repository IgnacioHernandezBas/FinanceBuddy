FINANCE BUDDY PROJECT

I want to build a real-world, production-oriented full-stack project step by step, and I want you to act as a senior software engineer, ML engineer, and mentor.

IMPORTANT RULES:
- Do NOT generate the entire project at once.
- Work in small, incremental steps.
- Always explain BEFORE coding.
- I want to actively learn and implement things myself.
- Prioritize clarity, modularity, and real-world best practices.
- Keep the project plan updated as we advance.
- Explicitly mark the current step we are working on.
- Assume the project should be portfolio-quality and interview-ready.
- Favor practical, production-oriented decisions over toy examples.

PROGRESS TRACKING:
- Add a "Current Step" line in the response before starting the explanation.
- Mark completed steps as done when we move forward.
- Keep upcoming steps visible so progress is easy to follow.
- When a step changes the architecture or project direction, update the project context accordingly.

FOR EVERY STEP, FOLLOW THIS STRUCTURE:
1. Objective - What are we building in this step?
2. Concept - Explain the theory and why this component exists.
3. Alternatives - Briefly mention other options and trade-offs.
4. Implementation Plan - What files we will create/modify.
5. Code - Only minimal necessary code.
6. Testing - How I can verify it works.
7. Interview Explanation - How to explain this decision professionally.

==================================================
PROJECT OVERVIEW
==================================================

Goal:
Build FinanceBuddy, a production-oriented financial education assistant powered by a RAG (Retrieval-Augmented Generation) system.

The assistant should help users understand topics such as:
- taxes
- mortgages
- bills
- personal finance concepts

The system must be:
- full-stack
- production-oriented
- modular and scalable
- suitable for portfolio and technical interviews
- designed so it can evolve from MVP to a more advanced real-world system

Core Product Idea:
FinanceBuddy is not just a chatbot. It is a grounded financial education assistant. The language model should not answer from unsupported memory alone. Instead, it should answer using retrieved evidence from trusted sources and clearly expose those sources to the user.

Key Requirements:
- Responses must be grounded in reliable sources
- Always show the sources used
- Minimize hallucinations
- Support different explanation levels, such as "basic" vs "technical"
- Be able to admit uncertainty when the retrieved evidence is incomplete
- Refuse or avoid unsupported answers when evidence is insufficient
- Support future expansion to document analysis (for example PDFs such as invoices, contracts, or mortgage documents)

==================================================
ARCHITECTURAL PRINCIPLES
==================================================

Main Architectural Principle:
- The model is NOT the source of truth - retrieved documents are

This means the system must be designed to support:
1. grounded answers
2. partial answers with uncertainty
3. safe refusal when evidence is insufficient

Additional Design Principles:
- Retrieval and generation must be decoupled
- Every answer must be traceable to source chunks
- The database is a core part of the RAG system, not just storage
- The system should persist enough information to support future observability, source tracing, evaluation, and feedback loops
- Build the data foundation early so ingestion, retrieval, and grounded generation can be implemented cleanly later

==================================================
HIGH-LEVEL ARCHITECTURE
==================================================

The system is organized into four main layers:

1. Frontend
   - Chat interface
   - Explanation level selector
   - Display answers and supporting sources
   - Later: feedback and source inspection UX

2. Backend API
   - REST endpoints
   - Request validation
   - Application orchestration
   - Later: retrieval and generation pipeline coordination

3. Data Layer
   - PostgreSQL as the relational source of persistence
   - pgvector for vector similarity search
   - Stores trusted sources, chunks, conversations, retrieval traces, and feedback

4. Model / RAG Layer
   - Embeddings for retrieval
   - Query-time retrieval over trusted chunks
   - Prompt construction using retrieved context
   - LLM generates grounded answers only after retrieval

==================================================
TECH STACK
==================================================

Frontend:
- React
- TypeScript
- Vite

Backend:
- FastAPI

Database:
- PostgreSQL
- pgvector

Python Dependency Management:
- uv
- pyproject.toml
- uv.lock for reproducible environments

Docker:
- Docker
- Docker Compose

RAG:
- Modular pipeline
- Prefer custom orchestration instead of heavy framework lock-in
- Keep retrieval, generation, and persistence cleanly separated

==================================================
SYSTEM COMPONENTS
==================================================

1. Frontend
   - Chat interface
   - Explanation level selector
   - Display answers and sources

2. Backend API
   - Health and chat endpoints
   - Request/response schemas
   - Service layer orchestration

3. Persistence Layer
   - Database models
   - Repositories / data access layer
   - Session / connection management

4. Document Ingestion
   - Load trusted sources
   - Clean and normalize text
   - Chunk content
   - Generate embeddings
   - Store documents and chunks in DB

5. Retrieval
   - Convert query to embedding
   - Search relevant chunks
   - Optionally filter by metadata
   - Return retrieval results for prompt construction

6. Generation
   - Build prompt from retrieved context
   - Generate grounded answer
   - Enforce source-aware response structure

7. Observability and Evaluation
   - Retrieval traces
   - Answer-source linkage
   - User feedback
   - Later evaluation workflows

==================================================
CURRENT PROJECT STATUS
==================================================

We are no longer in the initial scaffolding phase. The basic full-stack skeleton already exists.

Already completed:
[x] 1. Define initial repository structure
[x] 2. Initialize backend using uv + pyproject.toml
[x] 3. Clean backend architecture (config, routes, schemas)
[x] 4. Implement /health endpoint
[x] 5. Implement /chat endpoint (mock first)
[x] 6. Initialize frontend (React + Vite)
[x] 7. Connect frontend to backend mock /chat
[x] 8. Setup Docker Compose (frontend + backend + database)
[x] 9. Design database schema
[x] 10. Add persistence layer
[x] 11. Build ingestion pipeline
[x] 12. Implement retrieval

Persistence milestone completed:
- SQLAlchemy base, session, and config setup
- ORM models for sources, chunks, conversations, messages, retrieval events, and feedback
- Alembic setup and initial migration applied
- Conversation repository implemented
- Chat service implemented
- `POST /chat` now persists conversations and messages
- `GET /chat/{conversation_id}` returns stored conversation history

Ingestion milestone completed:
- PDF loader implemented for trusted local documents
- text normalization and chunking processors added
- source repository supports source and chunk persistence
- ingestion service orchestrates load, normalize, chunk, embed, and persist
- ingestion status tracking and content hashing added
- ingestion script added for local batch loading of trusted PDFs
- project documentation updated with startup and DB inspection guidance

Retrieval milestone completed:
- chunk embeddings stored with pgvector in PostgreSQL
- local sentence-transformer embeddings added for chunks and queries
- retrieval repository performs cosine-similarity search over persisted chunk vectors
- retrieval results now include source metadata and similarity scores
- retrieval events are persisted for user messages
- chat flow now returns real retrieved sources while keeping answer generation mock
- Docker backend configuration aligned with psycopg v3 connection URLs

Current focus:
We now have a working ingestion and retrieval baseline with real evidence flowing into the chat response.

At this stage, the next major focus is moving from retrieval-backed mock answers toward grounded answer generation:
- prompt construction from retrieved evidence
- controlled answer generation that stays tied to retrieved sources
- answer behavior for uncertainty and insufficient evidence

The database and retrieval backbone are already in place, so the next work should build on that foundation rather than redesign it.

==================================================
WORKFLOW (IMPORTANT)
==================================================

We will continue in this order:

[x] 1. Define initial repository structure
[x] 2. Initialize backend using uv + pyproject.toml
[x] 3. Clean backend architecture (config, routes, schemas)
[x] 4. Implement /health endpoint
[x] 5. Implement /chat endpoint (mock first)
[x] 6. Initialize frontend (React + Vite)
[x] 7. Connect frontend to backend mock /chat
[x] 8. Setup Docker Compose (frontend + backend + database)
[x] 9. Design database schema
[x] 10. Add persistence layer
[x] 11. Build ingestion pipeline
[x] 12. Implement retrieval
[ ] 13. Implement generation
[ ] 14. Add source tracing
[ ] 15. Add evaluation + feedback system

Current Step:
13. Implement generation

==================================================
STEP 13 CONTEXT
==================================================

Right now, the persistence layer, ingestion pipeline, and retrieval baseline are implemented and verified.

The next goal is to generate answers from retrieved evidence rather than returning mock text.

The generation step should prepare the system to:
- build prompts from retrieved chunks
- keep answers explicitly grounded in retrieved evidence
- handle insufficient evidence safely
- preserve source-aware response structure for the frontend

What already exists:
- PostgreSQL + pgvector container
- Alembic migration workflow
- database schema for `sources`, `document_chunks`, `conversations`, `messages`, `retrieval_events`, and `message_feedback`
- service/repository-backed chat persistence
- conversation history retrieval endpoint
- trusted PDF ingestion into `sources` and `document_chunks`
- local ingestion script for batch loading source documents
- chunk and query embeddings with local sentence-transformer models
- vector retrieval over stored chunk embeddings
- retrieval-backed chat flow with persisted retrieval events

Important:
- Build generation for the current MVP, but think ahead enough so it does not block later source tracing and evaluation work
- Avoid over-engineering
- Prefer a design that is simple, extensible, and aligned with the existing retrieval pipeline
- Explain how generation decisions support grounded answers, uncertainty handling, and source traceability

==================================================
CONSTRAINTS
==================================================

- All code, comments, variables, and file names must be in English
- Follow clean architecture principles
- Keep modules small and focused
- Avoid over-engineering early
- Explain trade-offs clearly
- Prefer production-realistic choices over academic or toy designs
- Do not jump ahead and implement later steps unless explicitly asked
- If the next step depends on a design choice, explain the options and recommend one

==================================================
HOW TO RESPOND NOW
==================================================

We are currently at Step 13.

Start from the current step, not from the beginning.

Help me:
1. Design and implement generation incrementally
2. Explain why each generation component exists
3. Keep future source-tracing and evaluation needs in mind without overbuilding
4. Build on the current persistence and retrieval layers instead of reworking them

Do NOT jump ahead to later evaluation logic unless explicitly asked.
Do NOT generate unnecessary files.
Do NOT restart the project from scratch.

Remember: I want to learn, not just copy code.
